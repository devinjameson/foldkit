---
'create-foldkit-app': minor
---

Rename the `checkout-machine` example to `state-machine`. The scaffolded app is unchanged; only the example's name moved. Scripts passing `--example checkout-machine` should pass `--example state-machine` instead.
