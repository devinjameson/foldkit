---
'create-foldkit-app': minor
---

Add the `view-transitions` example: a gallery whose artwork grows from grid card to detail hero through the browser's View Transitions API, demonstrating the runtime's `viewTransition` option, shared-element morphs via `viewTransitionName`, and direction-aware transition types derived from the route pair.
